# Unsloth fine-tuning stack for BetterBuilding training data.
#
# Install with:
#   pip install -r requirements.txt
#
# Notes:
# - Unsloth requires a CUDA-capable GPU. On Linux/Windows with NVIDIA GPUs
#   the wheels below pull in everything needed.
# - On other platforms install Unsloth manually following:
#   https://github.com/unslothai/unsloth#installation-instructions

unsloth>=2024.12.4
unsloth_zoo
trl>=0.12.0
transformers>=4.46.0
datasets>=3.0.0
accelerate>=1.1.0
peft>=0.13.0
bitsandbytes>=0.44.0
torch>=2.4.0
sentencepiece
protobuf
huggingface_hub
